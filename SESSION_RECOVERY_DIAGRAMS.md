# 🔄 Session Recovery - Visual Flowcharts & Diagrams

## 1️⃣ Session Lifecycle

```
┌──────────────────────────────────────────────────────────────┐
│                    SESSION LIFECYCLE                          │
└──────────────────────────────────────────────────────────────┘

┌─────────────┐
│   START     │
│  (Fresh)    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────┐
│ WorkoutPlayer Initializes       │
│ • Creates fresh exerciseSets    │
│ • workoutStartTime = Date.now() │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ stateInitializedRef = true      │
│ (After first render)            │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ STATE CHANGES:                  │
│ • User enters reps/weight       │
│ • User completes set            │
│ • User adds/removes set         │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ PERSIST TO localStorage:        │
│ localStorage.setItem(STORAGE_   │
│   KEY, JSON.stringify(...))     │
└──────┬──────────────────────────┘
       │
       ├─────────────────────────────────────┐
       │                                     │
       ▼                                     ▼
    ┌─────────┐                  ┌──────────────────┐
    │ CONTINUE│                  │INTERRUPTION      │
    │ WORKOUT │                  │(Reload/Close)    │
    └────┬────┘                  └────────┬─────────┘
         │                                 │
         ▼                                 ▼
    ┌─────────────────────┐      ┌──────────────────────┐
    │SESSION KEEPS UPDATING       │SESSION SAVED IN      │
    │AS USER PROGRESSES          │BROWSER STORAGE       │
    └────┬────────────────┘      └────────┬─────────────┘
         │                                 │
         ├─────────────────────────────────┤
         │                                 │
         ▼                                 ▼
    ┌──────────────┐             ┌──────────────────┐
    │USER FINISHES │             │USER REOPENS APP  │
    │OR CANCELS    │             │(Page Reload)     │
    └────┬─────────┘             └────────┬─────────┘
         │                                 │
         ▼                                 ▼
    ┌──────────────┐             ┌──────────────────┐
    │localStorage. │             │App Detects Session│
    │removeItem()  │             │in localStorage    │
    │(CLEANUP)     │             │(Recovery Check)   │
    └──────────────┘             └────────┬─────────┘
                                           │
                                           ▼
                                  ┌──────────────────┐
                                  │Parse sessionData │
                                  │Set recoveredSession
                                  │Navigate to 'home'│
                                  └────────┬─────────┘
                                           │
                                           ▼
                                  ┌──────────────────┐
                                  │Home Component    │
                                  │Detects Recovered │
                                  │Session via       │
                                  │useEffect         │
                                  └────────┬─────────┘
                                           │
                                           ▼
                                  ┌──────────────────┐
                                  │initializeState() │
                                  │Returns Recovered │
                                  │State             │
                                  └────────┬─────────┘
                                           │
                                           ▼
                                  ┌──────────────────┐
                                  │WorkoutPlayer     │
                                  │Renders with Full │
                                  │Recovered State   │
                                  │Timer Accurate    │
                                  └──────────────────┘
```

---

## 2️⃣ State Persistence Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  STATE PERSISTENCE                          │
└─────────────────────────────────────────────────────────────┘

USER ACTION                REACT STATE                LOCALSTORAGE
─────────────────────────────────────────────────────────────────

User enters                setExerciseSets()         (waiting for
weight                     ↓                          useEffect)
                     exerciseSets updated            
                           ↓
                     useEffect triggered
                           ↓
                     stateInitializedRef? ✓
                           ↓
                     Build sessionData
                           ↓
                     JSON.stringify()
                           ↓
                     localStorage.setItem()
                                                     ✓ SAVED
                                                     {
                                                       exerciseSets,
                                                       workoutStartTime,
                                                       indices...
                                                     }

(repeated for every state change)

User completes set        setExerciseSets()         updates in
                         handleSetComplete()        localStorage
                                ↓                   (same flow)
                         More sets?
                              ↓
                         NO → finishWorkout()
                              ↓
                         localStorage.removeItem()
                              ↓
                                                    ✓ CLEARED
```

---

## 3️⃣ Session Recovery Flow

```
┌──────────────────────────────────────────────────────────────┐
│              SESSION RECOVERY ON APP LOAD                     │
└──────────────────────────────────────────────────────────────┘

          ┌─────────────────────┐
          │   App Mounts        │
          │  (AuthProvider OK)  │
          └──────────┬──────────┘
                     │
                     ▼
          ┌─────────────────────┐
          │ useEffect (mount)   │
          │ Check localStorage  │
          └──────────┬──────────┘
                     │
                     ▼
          ┌──────────────────────────┐
          │ const savedSession =      │
          │   localStorage.getItem() │
          └──────────┬───────────────┘
                     │
                 ┌───┴───┐
                 │       │
                 ▼       ▼
              exists  null/empty
                 │       │
                 ▼       ▼
             ┌──────┐  ┌─────────┐
             │ TRY  │  │ NO      │
             │PARSE │  │ACTION   │
             └──┬───┘  └─────────┘
                │
            ┌───┴────┐
            │        │
            ▼        ▼
         SUCCESS   ERROR
           │         │
           ▼         ▼
        ┌─────┐   ┌──────────────┐
        │SET  │   │ CATCH ERROR  │
        │STATE│   │ Log & clear  │
        └──┬──┘   └──────────────┘
           │
           ▼
        ┌──────────────────────┐
        │ setRecoveredSession  │
        │   (sessionData)      │
        └─────────┬────────────┘
                  │
                  ▼
        ┌──────────────────────┐
        │ setCurrentPage       │
        │   ('home')           │
        └─────────┬────────────┘
                  │
                  ▼
        ┌──────────────────────┐
        │ Home Component       │
        │ Receives Props:      │
        │ • recoveredSession   │
        │ • onWorkoutComplete  │
        │ • etc...             │
        └─────────┬────────────┘
                  │
                  ▼
        ┌──────────────────────┐
        │ Home useEffect       │
        │ Watches for          │
        │ recoveredSession     │
        └─────────┬────────────┘
                  │
              ┌───┴───┐
              │       │
              ▼       ▼
           exists   null
             │       │
             ▼       ▼
        ┌────────┐ ┌──────────┐
        │ SET    │ │ SHOW     │
        │ACTIVE  │ │ HOME     │
        │WORKOUT │ │ NORMALLY │
        └────┬───┘ └──────────┘
             │
             ▼
        ┌──────────────────────┐
        │ WorkoutPlayer        │
        │ Renders              │
        │ Props:               │
        │ • workout (dummy)    │
        │ • recoveredSession   │
        └─────────┬────────────┘
                  │
                  ▼
        ┌──────────────────────┐
        │ initializeState()    │
        │ receives             │
        │ recoveredSession     │
        │ Returns Recovered    │
        │ State ✓              │
        └─────────┬────────────┘
                  │
                  ▼
        ┌──────────────────────┐
        │ All State Set:       │
        │ ✓ exerciseSets       │
        │ ✓ currentExerciseIdx │
        │ ✓ currentSetIdx      │
        │ ✓ workoutStartTime   │
        │ ✓ Timer Accurate!    │
        └──────────────────────┘
```

---

## 4️⃣ Timer Accuracy Guarantee

```
┌──────────────────────────────────────────────────────────────┐
│          TIMER ACCURACY WITH RECOVERY                         │
└──────────────────────────────────────────────────────────────┘

SCENARIO: Workout started at 12:00, interrupted at 12:10,
          resumed at 12:25


WRONG APPROACH (Saving elapsed seconds):
──────────────────────────────────────

12:00 - Start: elapsedSeconds = 0
12:05 - Save: elapsedSeconds = 300
12:10 - Interrupt: saved elapsedSeconds = 600
       (Phone off for 15 minutes)
12:25 - Resume: elapsedSeconds = 600 (WRONG! Should be ~1500)
       Timer shows 10 minutes, but actual time is 25 minutes


CORRECT APPROACH (Saving workoutStartTime):
─────────────────────────────────────────

12:00 - Start: workoutStartTime = Date.now() (ms)
12:05 - Save: workoutStartTime = SAME (original timestamp)
12:10 - Interrupt: saved workoutStartTime = 12:00 timestamp
       (Phone off for 15 minutes)
12:25 - Resume: 
       Calculate: Date.now() - workoutStartTime
       = 12:25 timestamp - 12:00 timestamp
       = 1500 seconds (25 minutes) ✓ CORRECT!


IMPLEMENTATION:
───────────────

const [workoutStartTime] = useState(initialState.workoutStartTime);

useEffect(() => {
  const interval = setInterval(() => {
    // This calculation works whether session is fresh or recovered
    setWorkoutDuration(Math.floor((Date.now() - workoutStartTime) / 1000));
  }, 1000);
  return () => clearInterval(interval);
}, [workoutStartTime]);

The beauty: workoutStartTime is the SAME value whether recovered or fresh!
           So the timer calculation always works correctly.
```

---

## 5️⃣ Data Structure Evolution

```
┌──────────────────────────────────────────────────────────────┐
│         SESSION DATA STRUCTURE IN LOCALSTORAGE                │
└──────────────────────────────────────────────────────────────┘

User starts: "Chest & Triceps" workout with:
  - Bench Press: 3 sets of 8-10 reps @ 100kg
  - Barbell Row: 3 sets of 6-8 reps @ 120kg


┌─ INITIAL STATE ─────────────────────────────────────────────┐
│ {                                                            │
│   workoutName: "Chest & Triceps",                           │
│   workoutStartTime: 1704474600000,  (Original timestamp)    │
│   currentExerciseIndex: 0,                                  │
│   currentSetIndex: 0,                                       │
│   exerciseSets: [                                           │
│     {                                                        │
│       name: "Bench Press",                                  │
│       targetSets: 3,                                        │
│       targetReps: "8-10",                                   │
│       targetWeight: "100",                                  │
│       sets: [                                               │
│         { id: 0, completed: false, reps: "", weight: "" },  │
│         { id: 1, completed: false, reps: "", weight: "" },  │
│         { id: 2, completed: false, reps: "", weight: "" }   │
│       ]                                                      │
│     },                                                       │
│     { ... Barbell Row ... }                                 │
│   ]                                                          │
│ }                                                            │
└─────────────────────────────────────────────────────────────┘


┌─ AFTER SET 1 OF BENCH PRESS ────────────────────────────────┐
│ {                                                            │
│   workoutName: "Chest & Triceps",                           │
│   workoutStartTime: 1704474600000,  (UNCHANGED - Critical!) │
│   currentExerciseIndex: 0,                                  │
│   currentSetIndex: 1,  (Moved to set 2)                     │
│   exerciseSets: [                                           │
│     {                                                        │
│       name: "Bench Press",                                  │
│       targetSets: 3,                                        │
│       targetReps: "8-10",                                   │
│       targetWeight: "100",                                  │
│       sets: [                                               │
│         { id: 0, completed: true, reps: "10", weight: "100"},
│         { id: 1, completed: false, reps: "", weight: "" },  │
│         { id: 2, completed: false, reps: "", weight: "" }   │
│       ]                                                      │
│     },                                                       │
│     { ... Barbell Row ... }                                 │
│   ]                                                          │
│ }                                                            │
└─────────────────────────────────────────────────────────────┘


┌─ AFTER COMPLETION (localStorage.removeItem) ────────────────┐
│                                                              │
│  Key "trackd_active_session" is DELETED from storage        │
│  Next time app loads, no recovery will occur                │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 6️⃣ Error Handling Flowchart

```
┌──────────────────────────────────────────────────────────────┐
│            ERROR HANDLING STRATEGY                            │
└──────────────────────────────────────────────────────────────┘

SAVING SESSION:
───────────────
try {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(sessionData))
}
catch (error) {
  // Quota exceeded? Storage disabled? Permission denied?
  // → Log the error
  // → Workout continues normally (just not saved)
  // → User warned via console
  console.error('Error saving session:', error)
}


RECOVERING SESSION:
───────────────────
try {
  const savedSession = localStorage.getItem(STORAGE_KEY)
  ├─ null? → Skip (no session to recover)
  └─ string? → JSON.parse(savedSession)
                ├─ Valid JSON? → Use sessionData ✓
                └─ Invalid? → throw SyntaxError
}
catch (error) {
  // Could be:
  // 1. JSON parsing failed (corrupted data)
  // 2. Unexpected data structure
  // 3. Browser security restrictions
  
  → Log error
  → localStorage.removeItem(STORAGE_KEY)  // Cleanup
  → App loads normally without recovery
  → User gets fresh workout
  console.error('Error recovering session:', error)
  localStorage.removeItem(STORAGE_KEY)
}


CLEARING SESSION:
─────────────────
try {
  localStorage.removeItem(STORAGE_KEY)
}
catch (error) {
  // Very unlikely but:
  // → Log if something goes wrong
  // → App continues normally
  console.error('Error clearing session:', error)
}


RESULT: No matter what goes wrong, app continues to function!
```

---

## 7️⃣ Component Integration Map

```
┌──────────────────────────────────────────────────────────────┐
│         COMPONENT INTEGRATION & DATA FLOW                     │
└──────────────────────────────────────────────────────────────┘


                          ┌─────────────┐
                          │   App.jsx   │
                          └──────┬──────┘
                                 │
                    ┌────────────┼────────────┐
                    │            │            │
                    ▼            ▼            ▼
        ┌─────────────────┐  ┌──────────┐  ┌─────────┐
        │ Check for saved │  │ Set      │  │ Pass to │
        │ session on      │  │recovered │  │ Home    │
        │ mount           │  │Session   │  │ props   │
        └─────┬───────────┘  │ state    │  └────┬────┘
              │              └──────────┘       │
              │                                 │
              └─────────────────┬───────────────┘
                                │
                                ▼
                          ┌────────────┐
                          │ Home.jsx   │
                          └─────┬──────┘
                                │
                    ┌───────────┼────────────┐
                    │           │            │
                    ▼           ▼            ▼
            ┌────────────┐  ┌──────────┐  ┌─────────────┐
            │ Detect     │  │ Set      │  │ Pass        │
            │ recovered  │  │active    │  │ recovered   │
            │ session    │  │workout   │  │ session to  │
            │ in useEffect  │          │  │ WorkoutPlayer
            └────┬───────┘  └──────────┘  └────┬────────┘
                 │                              │
                 └──────────────┬───────────────┘
                                │
                                ▼
                        ┌──────────────────┐
                        │ WorkoutPlayer.jsx│
                        └────────┬─────────┘
                                 │
                    ┌────────────┼──────────────┐
                    │            │              │
                    ▼            ▼              ▼
            ┌─────────────┐  ┌──────────┐  ┌────────────┐
            │ Initialize  │  │ Persist  │  │ On finish/ │
            │ state from  │  │ to       │  │ cancel:    │
            │ recovered   │  │localStorage clearitem()  │
            │ session     │  │on changes│  │            │
            │ OR fresh    │  │          │  │            │
            └─────────────┘  └──────────┘  └────────────┘
```

---

## 8️⃣ Comparison: Before vs After

```
┌──────────────────────────────────────────────────────────────┐
│              BEFORE vs AFTER                                  │
└──────────────────────────────────────────────────────────────┘

BEFORE SESSION RECOVERY:
────────────────────────

User starts workout
  ↓
Page reload (accidental)
  ↓
❌ ALL PROGRESS LOST
  ↓
Frustrated user starts over
  ↓
Negative experience


AFTER SESSION RECOVERY:
───────────────────────

User starts workout
  ↓
App automatically saves state to localStorage
  ↓
Page reload (accidental)
  ↓
✅ App DETECTS saved session
  ✅ Automatically NAVIGATES to home with active session
  ✅ WorkoutPlayer RENDERS with full state restored
  ✅ Timer shows CORRECT elapsed time
  ✅ User resumes from EXACT POINT
  ↓
Happy user continues workout
  ↓
Positive experience (app "remembers" everything!)


IMPACT:
───────
User Trust:        ⬆️⬆️⬆️ (App feels intelligent & reliable)
User Frustration:  ⬇️⬇️⬇️ (No more lost progress)
App Reliability:   ⬆️⬆️⬆️ (Handles interruptions gracefully)
User Retention:    ⬆️⬆️⬆️ (Users feel supported)
```

---

## Summary

These diagrams show:
- ✅ Complete session lifecycle
- ✅ Persistence mechanism
- ✅ Recovery flow
- ✅ Timer accuracy guarantee
- ✅ Error handling
- ✅ Component integration
- ✅ User experience improvement

All working together to create a robust Session Recovery feature! 🚀
